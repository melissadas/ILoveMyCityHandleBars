<div class="container">
          <div class="row">
            {{#each imageArray}}
               <div class="col-md-6">
                  <div class="thumbnail">
                  <img src="/images/{{../city}}/{{../city}}-{{this}}.jpg" class="img-size"/>
                  </div>
               </div>
            {{/each}}
          </div>
</div>
